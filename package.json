{
  "name": "svgpanzoom",
  "version": "1.0.0",
  "description": "jQuery plugin to pan and zoom SVG images either programatically or through mouse/touch events",
  "main": "src/index.js",
  "scripts": {
    "start": "babel src/index.js --watch --out-file dist/SVGPanZoom.js",
    "build": "babel src/index.js --out-file dist/SVGPanZoom.js"
  },
  "keywords": [
    "jquery",
    "svg",
    "pan",
    "zoom",
    "touch",
    "pinch"
  ],
  "author": "AvcS",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "jquery": "^3.3.1"
  }
}
